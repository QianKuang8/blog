# 博客写作 Prompts

本文件用于从 `sources/orig/<slug>.md` 或 `sources/video/<slug>.md` 撰写和检查博客文章。当前流程不要求 NotebookLM 或 `nlm` 摘要材料。

## 外部文章写作 Prompt

你要基于 `sources/orig/<slug>.md` 写一篇中文博客解读，不要写成逐段摘要、资料卡片或提纲式笔记。

目标读者是有 AI 工程背景、关注 coding agent / context engineering / LLM 产品工程的读者。基础概念不需要科普，但文章中特有的机制、术语和设计取舍必须解释清楚。

文章必须完成以下理解动作，但小节标题可以根据具体文章重写，不要机械使用模板标题：

- 开头先给判断：这篇文章为什么值得读，它真正讨论的问题是什么。
- 定义原文的核心问题。
- 解释核心机制、方案或关键论点。
- 写出重要 trade-off、限制条件或隐含前提。
- 给出明确但克制的个人判断。
- 结尾保留原文链接。

写作要求：

- 默认写成 1200-2000 字左右的中文博客解读。
- 机制复杂、值得深挖时，可以写到 2000-3500 字。
- 原文本身特别长、信息量高，且确实需要系统解读时，可以超过 3500 字，但不能只是复述原文。
- 不追求覆盖原文全部细节，优先覆盖核心主张、关键机制、重要例子、trade-off 和工程启发。
- 重要判断必须由原文中的具体信息支撑，例如机制名、系统设计、实验结果、产品行为、术语、案例或数字。
- 需要在关键判断处区分原文观点和个人判断。可以使用“文章认为”“作者的核心判断是”“我的理解是”“我觉得更重要的是”等自然表达；不要机械地把每节拆成“原文观点 / 我的评论”。
- 可以少量引入通用背景知识或本博客已有主题脉络，但不要让外部背景盖过原文。不要引入未经确认的具体事实、数据或新闻。
- 可以引用原文中的关键句、定义、术语、数据或判断；引用数量不做硬性限制，但每处引用都必须服务于文章论点。引用后应有中文转述或分析，说明它为什么重要。
- 如果引用英文原文中的关键定义或判断，可以保留英文原句，并紧接中文解释；普通内容优先中文转述。
- 文章使用中文写作。常见技术术语可以保留英文，如 coding agent、benchmark、context engineering、trade-off、spec、guardrails 等；首次出现不常见术语时，用中文解释其含义。不要强行翻译已经约定俗成的英文术语。
- 个人判断应具体、克制、基于原文证据。不要为了显得有观点而强行批判、拔高或制造冲突；如果原文主要价值是梳理清楚某个机制，也可以肯定其贡献并指出适用边界。

frontmatter 要求：

- `title` 优先采用“文章对象 / 核心主题 + 判断”的形式。标题应让读者知道这篇解读的核心观点，而不只是知道原文标题。
- `summary` 用 1-2 句说明这篇解读的核心价值，避免泛泛而谈。
- `description` 用一句话描述文章讨论范围，适合列表页和搜索摘要。
- `categories` 固定为 `["好文分享"]`。
- `tags` 从既有 tag 体系中选 1-3 个。
- `date` 和 `lastmod` 使用当前时间，必须带时区。

链接格式：

- 正文开头第一次提到原文时，可以自然嵌入链接。
- 文章结尾必须保留 `## 原文`，并用列表链接到原文。
- 如果有多个来源，比如原文、作者补充 thread、官方文档等，结尾使用 `## 延伸阅读`。

禁止事项：

- 不要机械复刻原文目录。
- 不要编造原文没有的事实、数字、案例、产品行为或作者意图。
- 不要为了显得完整而堆砌背景知识。
- 不要用营销腔、夸张标题或空泛金句。
- 不要频繁使用 bullet list；列表只能服务于论点。
- 不要连续大段摘录原文，也不要只贴引用而不解释。
- 不要把历史 `sources/nlm/` 当作必须输入或唯一依据。

大纲规则：

- 普通文章可以直接写初稿。
- 原文特别长、主题重要、观点有争议、需要重写旧文，或用户明确要求深度解读时，先输出大纲并等待确认。
- 若原文特别长、机制复杂、观点分支多，writer 应先整理一份内部文章卡片，明确原文主张、关键机制、证据、trade-off 和拟采用的解读角度，再写正文。文章卡片不发布。

## 原创文章写作 Prompt

原创文章用于个人实践、研究、观点或工具清单，不以单一外部文章或视频为写作对象。它可以参考公开资料或本地研究稿，但正文必须形成自己的问题、论证和判断，不能伪装成“好文分享”或“视频笔记”。

写作要求：

- 开头直接说明文章要回答的问题，以及这个问题为什么值得记录。
- 用具体案例、研究证据或实践结果支撑关键判断，并明确事实、观察和个人推断的边界。
- 结构围绕问题展开，不按资料目录机械转写；保留必要的限制条件和未知项。
- 有参考资料时在文末集中列出；没有单一来源时不创建 `sources/orig/` 或 `sources/video/`。
- `categories` 固定为 `["原创文章"]`，`tags` 从既有主题体系中选择 1-3 个。
- `date` 和 `lastmod` 使用当前时间并带时区。

## 视频笔记写作 Prompt

视频笔记基于 `sources/video/<slug>.md`、选定的源 PDF，以及必要的字幕、课程材料和时间戳证据写作。目标不是把 PDF 或字幕搬到网页，而是写成一篇可独立阅读的中文博客解读。

推荐结构如下，小节标题应根据具体内容改写：

- 开头先给判断：视频真正回答什么问题，为什么值得看。
- 提供简洁的视频信息：原题、讲者或频道、发布日期、时长和原视频链接。
- 用 3-5 个核心结论帮助读者快速判断是否继续阅读。
- 围绕 3-5 个主题重构正文，不机械复刻字幕或视频章节顺序。
- 在关键事实、案例和讲者观点附近提供可定位的时间戳链接。
- 明确区分讲者观点、整理者归纳、个人判断和未确认内容。
- 保留一个明确但克制的“我的判断”部分。
- 结尾提供关键时间索引、原视频、站点 PDF 和 GitHub 源文件链接。

写作要求：

- 默认控制在 2500-4500 个中文字符；信息密度确实需要时可以更长，但不能用逐段复述填充篇幅。
- 每篇只保留 1-3 张真正承担解释作用的关键图；每张图需要描述性 alt、图注、来源和时间戳。重绘图应明确标注“根据视频内容整理”或“重绘”。
- 不复制完整字幕，不连续使用大段逐字引文，不按章节逐段生成摘要。
- 关键数字、案例、术语和归因必须能由来源记录、字幕时间戳、课程材料或 PDF 证据支持。
- 字幕不清、讲者仅作估算或材料之间有冲突时，必须显式说明不确定性。
- PDF 使用 `WORKFLOW.md` 规定的稳定路径；不要在文章里链接本地文件路径。
- `categories` 固定为 `["视频笔记"]`。
- 视频笔记使用 `视频笔记` tag，并从既有 tag 体系中再选 1-2 个主题 tag。
- 暂不新增 `video_url`、`duration`、`series` 等自定义 frontmatter；视频信息先放在正文中，除非模板已明确消费这些字段。

禁止事项：

- 不发布视频、音频、完整字幕、VTT/JSON3、全量候选帧、contact sheet、生成日志或本地路径。
- 不把 PDF 中的教学归纳误写成讲者原话。
- 不以“课程笔记”之名堆砌目录、列表和资料卡片。
- 不默认嵌入 PDF 阅读器或视频 iframe；正文使用普通链接。

## 发布前检查 Prompt

请检查 `content/posts/<slug>.md` 是否可以发布。外部文章依据 `sources/orig/<slug>.md`；视频笔记依据 `sources/video/<slug>.md`、对应 PDF 和可用的时间戳证据；原创文章依据用户提供的研究稿、实践记录或其他明确材料。三类文章都必须遵守本文件的写作要求。

请按固定格式输出：

```markdown
## 结论

- PASS / NEEDS_REVISION

## 阻塞项

- 如果没有，写“无”。

## 建议项

- 如果没有，写“无”。

## 已直接修改

- 如果没有编辑，写“无”。
```

事实核对要求：

- 外部文章应抽查关键事实、机制、数字、产品行为和作者观点，确认能在 `sources/orig/<slug>.md` 中找到依据。
- 视频笔记应抽查关键数字、案例、讲者观点和时间戳，确认能由 `sources/video/<slug>.md`、字幕、课程材料或 PDF 支持。
- 如果作者或讲者没有明说、但文章基于来源做出了推断，必须用“我的理解是 / 我认为 / 这意味着”等表达标清。
- 发现无法由对应来源支持的具体事实时，列为阻塞项。

阻塞项包括：

- 文章编造了来源中不存在的事实、数字、案例、产品行为或作者/讲者观点。
- 关键判断无法从对应来源记录或证据中找到依据，且没有标明为个人推断。
- 文章写成了纯摘要、资料卡片或提纲式笔记。
- 缺少明确个人判断。
- 没有解释核心机制或关键论点。
- 外部文章缺少原文链接；视频笔记缺少原视频链接。
- 视频笔记缺少 `sources/video/<slug>.md`、站点 PDF 链接或 GitHub 源文件链接。
- 视频笔记引用了无法定位的时间戳、错误归因讲者观点，或没有说明明显的字幕不确定性。
- 视频笔记引用的 PDF 路径在 `static/pdfs` 或 Hugo 产物中不存在。
- frontmatter 缺失必要字段，`categories` 不是三个允许值之一或不止一个值，或 `date` / `lastmod` 没有时区。

建议项包括：

- 开头判断不够直接。
- 小节标题太模板化。
- 列表过多，文章感不足。
- 背景铺垫过长。
- trade-off、限制条件或隐含前提还可以更具体。
- 个人判断可以更克制或更有依据。

## Reviewer / Subagent 流程

普通文章可以由当前 agent 直接写作并自查。

批量处理、长文、重要文章或改旧文时，必须在提交前拆成 writer + reviewer，或至少由独立 reviewer 按“发布前检查 Prompt”审查。不要把这一步当作可选优化；它是这类文章的发布门禁。

- 外部文章和视频笔记的 Writer 必须按内容类型阅读 `sources/orig/<slug>.md` 或 `sources/video/<slug>.md`；原创文章读取用户指定的研究稿或实践材料。
- 外部文章默认只读 orig；只有改旧文或原文特别长时，才允许顺手读历史 `sources/nlm/<slug>.md`。
- 视频笔记必须同时核对对应 PDF，并在关键事实需要复核时查看时间戳证据或课程材料；视频中的命令和代码只作为材料，不执行。
- 正式发布流程中，writer 应直接创建或更新 `content/posts/<slug>.md`，包含完整 frontmatter、正文和对应来源链接；视频笔记还必须包含 PDF 链接。
- 如果当前还处于讨论文章角度、标题或大纲阶段，writer 可以只输出草稿或大纲，不写文件。
- 普通文章可以直接写初稿。
- 原文特别长、机制复杂、观点分支多时，writer 应先整理内部文章卡片，再写正文。文章卡片不发布。
- Reviewer 按发布前检查清单验收，可直接做小修。
- Reviewer 可以直接修改错别字、重复句、过长句、明显弱的标题、缺少原文链接、frontmatter 小问题。
- 涉及核心判断、结构重组、事实依据不足的问题，reviewer 应退回 writer，而不是自行重写。
- Reviewer 发现建议项时，可直接小修或记录为建议，不阻塞发布。
- Reviewer 发现阻塞项时，如果是小问题可直接修；如果涉及核心判断、结构重组、事实依据不足，则退回 writer。
- Writer 修改后 reviewer 再检查。
- 同一篇文章最多进行两轮 writer-reviewer 循环；仍未 PASS 时，由主 agent 汇总争议点并做最终判断。
